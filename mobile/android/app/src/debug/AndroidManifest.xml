<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Debug builds only: allow cleartext to any host so a physical device can
         reach a backend on the developer's LAN IP without editing source. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:usesCleartextTraffic="true"
        tools:replace="android:usesCleartextTraffic,android:networkSecurityConfig"
        android:networkSecurityConfig="@xml/network_security_config_debug"
        xmlns:tools="http://schemas.android.com/tools"/>
</manifest>
