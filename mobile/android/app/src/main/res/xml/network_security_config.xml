<?xml version="1.0" encoding="utf-8"?>
<!--
  Release: HTTPS only (cleartext disabled) — deployment never depends on plain
  HTTP. Local development uses cleartext to the two backend hosts only
  (emulator loopback 10.0.2.2, and localhost); a physical-device build points
  at an HTTPS URL or a LAN IP added here / via debug overlay.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
